import React from 'react'
import ReactDOM from 'react-dom/client'
import RoutesWeb from './routes.tsx'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <RoutesWeb />
  </React.StrictMode>,
)
